import React from 'react';
import {Text, TouchableOpacity, View} from 'react-native';

const Button = (): React.JSX.Element => {
  return (
    <TouchableOpacity>
      <Text>concmeeeeee</Text>
    </TouchableOpacity>
  );
};

export default Button;
